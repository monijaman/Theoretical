# Advanced TypeScript for Frontend

## 📚 Learn

### Type System Mastery

- **Advanced generics**
  - Generic constraints
  - Generic defaults
  - Generic factories and patterns

- **Conditional types**
  - Type narrowing based on conditions
  - Distributive conditional types
  - Extracting type information

- **Infer keyword**
  - Extracting types from complex structures
  - Function signature inference
  - Pattern matching with infer

### Type Safety Patterns

- **Utility types creation**
  - Building custom utility types
  - Mapped types
  - Type transformations

- **Discriminated unions**
  - Type guards for unions
  - Exhaustiveness checking
  - Safe pattern matching

- **Type-safe API layer**
  - Request/response typing
  - Error handling types
  - Endpoint-specific types

- **Zod schema validation**
  - Runtime validation
  - Type inference from schemas
  - Combining Zod with TypeScript

---

## 🛠️ Build

### Hands-On Projects

**1. Fully type-safe API client**

- Implement type-safe HTTP client
- Auto-complete endpoints and parameters
- Type-safe response handling
- Error type discrimination

**2. Type-safe form validation system**

- Generic form data types
- Field-level validation types
- Custom validation rules with types
- Integration with Zod

**3. Reusable generic table component**

- Generic row/column typing
- Type-safe sorting and filtering
- Props inference from data types
- Extensible column definitions

---

## 🎯 Goal

> **Move runtime bugs → compile-time errors.**

The goal of advanced TypeScript is to catch issues at the editor level before they ever reach users.

### You Should Master

- Why does this type error exist?
- How do I write a generic that prevents this bug?
- Can I make this impossible to misuse with types?
- What's the simplest type-safe solution?

---

**Let's make TypeScript work for you! 💪**
