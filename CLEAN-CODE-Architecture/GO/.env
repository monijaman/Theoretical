# Database Configuration
DB_HOST=localhost
DB_PORT=5428
DB_USER=root
DB_PASSWORD=root
DB_NAME=authdb

# Legacy DATABASE_URL (if needed by other parts)
DATABASE_URL=postgres://root:root@localhost:5428/authdb?sslmode=disable

# Application Configuration
GO_ENV=development
PORT=8080

# JWT Configuration
JWT_SECRET=msservice_jwt_key

# Kafka Configuration
KAFKA_BROKER=localhost:9092
